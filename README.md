WebChat Fullstack Prototype

Run backend and frontend separately.

Backend (in backend/):

```bash
cd backend
npm install
npm run dev
```

Frontend (in frontend/):

```bash
cd frontend
npm install
npm run dev
```

The frontend expects backend at http://localhost:4000 and frontend dev server runs on 5173.
